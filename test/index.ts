// tslint:disable: only-arrow-functions
import { expect } from "chai";

describe("Index module", function() {
    describe("expected behavior", function() {
        it("should serve as test template", function() {
            expect(1).to.equal(1);
        });
    });
});